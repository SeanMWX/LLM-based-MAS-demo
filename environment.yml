name: py12langgraph
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
      - langgraph>=1.1,<2
      - langchain>=1.2,<2
      - langchain-core>=1.2,<2
      - langchain-openai>=1.1,<2
      - langchain-anthropic>=1.4,<2
      - anthropic>=0.86,<1
      - pytest>=8,<9
